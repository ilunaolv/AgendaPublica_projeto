@*
    menu lateral (sidebar).

*@

<div id="sidebar-menu" class="d-flex flex-column flex-shrink-0 p-3 text-dark bg-light" style="width: 250px;">
    <ul class="nav nav-pills flex-column mb-auto">
        <li class="nav-item">
            <a asp-controller="Home" asp-action="Index" class="nav-link @(ViewContext.RouteData.Values["Action"].ToString() == "Index" ? "active" : "text-dark")" aria-current="page">
                <i class="bi bi-house-door-fill me-2"></i>
                Dashboard
            </a>
        </li>
        <li>
            <a asp-controller="Home" asp-action="criarEvento" class="nav-link @(ViewContext.RouteData.Values["Action"].ToString() == "criarEvento" ? "active" : "text-dark")">
                <i class="bi bi-calendar-week-fill me-2"></i>
                Criar Evento
            </a>
        </li>
        <li>
            <a asp-controller="Home" asp-action="gerarRelatorio" class="nav-link @(ViewContext.RouteData.Values["Action"].ToString() == "gerarRelatorios" ? "active" : "text-dark")">
                <i class="bi bi-file-bar-graph-fill me-2"></i>
                Gerar Relatórios
            </a>
        </li>
    </ul>
</div>